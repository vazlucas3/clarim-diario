// Arquivo carregado AUTOMATICAMENTE antes de cada teste (config no vite.config).
// jest-dom adiciona "matchers" extras e legíveis ao expect(), como
// toBeInTheDocument(), toHaveTextContent(), etc.
import '@testing-library/jest-dom'